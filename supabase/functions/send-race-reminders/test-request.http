# Test: send-race-reminders Edge Function
# Replace YOUR_PROJECT_REF with your Supabase project ref (Dashboard → Settings → General).
# Replace YOUR_ANON_KEY with your VITE_SUPABASE_ANON_KEY from .env
# Function finds races 24–26h from now and emails all auth users (or returns "No races due for reminder").

POST https://YOUR_PROJECT_REF.supabase.co/functions/v1/send-race-reminders
Authorization: Bearer YOUR_ANON_KEY
Content-Type: application/json
